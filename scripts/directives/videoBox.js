// app.directive('videoBox', function($http, $compile) {
//   return {
//
//       var strElm = `<section>
//         <h3>${video.title}</h3>
//         <div ng-click="addToWatched(video.id)">${video.embed_code}</div>
//       </section>`;
//       var compiledHtml = $compile(strElm);
//       // element.append(compiledHtml);
//       break;
//   };
// });
